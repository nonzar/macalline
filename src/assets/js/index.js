$(document).ready(function () {
  $('.m-banner .owl-carousel').owlCarousel({
    loop: true,
    items: 1
  })
  $('.m-branch .owl-carousel').owlCarousel({
    center: true,
    items: 1.2,
    loop: true,
    margin: 10
  })
})